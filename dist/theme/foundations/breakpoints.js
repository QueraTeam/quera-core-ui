import { createBreakpoints } from "@chakra-ui/theme-tools";

export var breakpoints = createBreakpoints({
    sm: "576px",
    md: "768px",
    lg: "992px",
    xl: "1200px",
    "2xl": "1600px",
});
